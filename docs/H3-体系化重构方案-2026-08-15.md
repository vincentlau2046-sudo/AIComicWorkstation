# H3 Prompt 体系化重构 — 详细方案

> 日期: 2026-08-15  
> 状态: 待审核  
> 范围: H3 目录架构 + 提示词库 Registry 集成 + FL2V P0/P1 优化

---

## 1. 现状分析

### 1.1 当前架构图

```
┌─────────────────────────────────────────────────────────────┐
│ video-generate.ts (pipeline) / route.ts (3 处)              │
│   resolvePrompt("video_h3_prompt")  → 系统 prompt 覆盖      │
│   buildVideoPromptLLM(input, provider, systemOverride)       │
│     └→ h3/index.ts                                          │
│         └→ h3/base-mode.ts  (buildH3BasePromptLLM)          │
│             └→ h3/prompt-template.ts                        │
│                 ├── buildContentLayer(input, lang)           │
│                 │   ├─ VIDEO SCRIPT ✅                       │
│                 │   ├─ CHARACTERS ✅                         │
│                 │   ├─ DIALOGUE SCRIPT ✅                    │
│                 │   ├─ FRAME ANCHORS (含完整环境) ❌          │
│                 │   ├─ SCENE (sceneDescription/Lighting) ❌   │
│                 │   ├─ EPISODE CONTEXT ✅                    │
│                 │   └─ AUDIO ✅                              │
│                 ├── buildConstraintLayer(input, lang)         │
│                 │   ├─ 时间结构 + 动作节拍 + 对白格式        │
│                 │   ├─ 运镜约束 + 格式约束                   │
│                 │   └─ ❌ 无 FL2V 专属规则（禁止环境/运镜优先）│
│                 └── buildGuideLayer(lang)                    │
│                     └─ ROLE + TASK + PROCESS + KEY PRINCIPLES│
│         └→ h3/ref-mode.ts  (generationMode="reference")      │
│             └→ 复用 base-mode 的 buildH3BasePrompt(Local)    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ registry.ts — 提示词库                                       │
│   video_generate        → Seedance 风格 video prompt（旧路径）│
│   ref_video_generate    → 参考图模式 video prompt（旧路径）   │
│   video_h3_prompt       → H3 system prompt 覆盖（单一条目）  │
│   ❌ H3 content layer 硬编码在 prompt-template.ts            │
│   ❌ H3 constraint rules 硬编码在 prompt-template.ts         │
│   ❌ 无 FL2V/R2V/T2V 模式区分                               │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 核心问题

| # | 问题 | 位置 | 后果 |
|---|------|------|------|
| 1 | **硬编码** — Content/Constraint Guide 层直接写在 `prompt-template.ts` 字符串中 | `prompt-template.ts` | 无法通过 WebUI 编辑，无法 A/B 测试 |
| 2 | **模式混杂** — "base-mode" 实际是 FL2V，但名称不透明 | `base-mode.ts` | R2V 误复用会导致 driver 错位 |
| 3 | **单项覆盖** — `video_h3_prompt` 只有一个 registry entry | `registry.ts` L1956 | R2V/T2V 无法独立配置 system prompt |
| 4 | **环境冗余** — SCENE + FRAME ANCHORS 环境信息三倍传入 | `prompt-template.ts` buildContentLayer | LLM 复述导致 token 浪费 |

---

## 2. 目标架构

### 2.1 双系统联动

```
┌──────────────────────────────────────────────────────────────┐
│                    Registry (提示词库)                        │
│  video_h3_fl2v_guide       → FL2V Guide Layer (role/task)    │
│  video_h3_fl2v_content     → FL2V Content Layer labels       │
│  video_h3_fl2v_constraints → FL2V Constraint rules           │
│  video_h3_fl2v_narration   → FL2V 旁白生成 system prompt     │
│  video_h3_r2v_guide        → R2V Guide Layer                 │
│  video_h3_r2v_ref          → R2V Ref2VA builder rules        │
│  video_h3_t2v_guide        → T2V Guide Layer (stub)          │
└──────────────────────────────────────────────────────────────┘
                          ↑
                resolvePrompt("video_h3_fl2v_constraints")
                resolveSlotContents("video_h3_fl2v_content")
                          │
┌──────────────────────────────────────────────────────────────┐
│                    h3/ (运行时引擎)                           │
│                                                              │
│  index.ts  ← 模式分发器（不变 API）                          │
│  types.ts camera-map.ts language-route.ts  ← 共享            │
│                                                              │
│  shared/                                                     │
│    base-builder.ts  ← buildH3Sections() 本地格式化           │
│                                                              │
│  fl2v/                                                       │
│    builder.ts          ← buildFL2VPromptLLM (读取 registry)  │
│    prompt-template.ts  ← 3-layer 组装（读取 registry slots） │
│    narration-gen.ts    ← 旁白自动生成（读取 registry）       │
│                                                              │
│  r2v/  ref-builder.ts  ← 6-section Ref2VA                    │
│  t2v/  index.ts        ← 占位                                │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 关键设计原则

> **Registry 是"配置"，h3/ 是"引擎"**。所有可编辑的 prompt 文本下沉到 Registry 的 Slot 体系，h3/ 代码只负责组装和注入动态数据。

---

## 3. Registry 扩展 — 提示词库条目设计

### 3.1 新增 6 个 PromptDefinition

#### 3.1.1 `video_h3_fl2v_guide` — FL2V Guide Layer

```typescript
// 将 prompt-template.ts buildGuideLayer() 的静态文本拆为 slots

const FL2V_GUIDE_ROLE = `## 角色
你是 MiniMax H3 FL2VA 模式的专家级提示词工程师。FL2VA = First-Last-Frame-to-Video/Audio：
模型接收首帧图片（<Picture 1>）+ 尾帧图片（<Picture 2>），自动插值生成中间视频帧和同步音频。

你的任务：将提供的视频剧本+角色+对白上下文，转换为描述"首帧→尾帧过渡路径"的 H3 结构化提示词。`;

const FL2V_GUIDE_TASK = `## 任务
重要——FL2VA 模式下，首尾帧图片已经提供了全部视觉锚点（角色外观、场景环境、光线、色调）。
你的 prompt 不需要描述这些已存在的内容。你只需要描述：
1. 从 Picture 1 到 Picture 2 之间发生了什么变化
2. 运镜如何移动
3. 角色的身体动作（具体物理动词，不用抽象词）
4. 对白/旁白/画外音
5. 音效和 BGM`;

const FL2V_GUIDE_PROCESS = `## 流程
1. 阅读视频剧本——理解叙事核心和情绪走向
2. 阅读角色列表——分配 speaker ID (S1, S2...)，确认谁在何时说话
3. 阅读对话台本——嵌入对应时间段
4. 按约束规则中的时间段拆分——每段写 [Camera: ...] + 角色动作 + 对白
5. 如对白为空 → 必须主动生成画外音或旁白
6. 生成完整的 H3 提示词`;

const FL2V_GUIDE_PRINCIPLES = `## 关键原则
- FL2V 是"过渡路径"模型，不是"场景说明书"——只描述变化，不描述静态
- 每秒必须有视觉变化：光线移动、物体微动、角色微表情、镜头微调
- 首帧→末帧过渡必须是因果推进：先发生A→随即触发B→然后导致C
- 运镜必须绑定到具体时间段——不能只写一个方向粘在末尾
- 用具体物理动词（转头/握拳/后退），禁用抽象词（陷入沉思/神情变化）
- 无对白 ≠ 静默——补画外音/内心独白，每 3-5s 至少一句声音`;

const FL2V_GUIDE_OUTPUT = `## 输出
仅输出 H3 格式内容。无前言、无 markdown、无注释。`;

const fl2vGuideDef: PromptDefinition = {
  key: "video_h3_fl2v_guide",
  category: "video",
  slots: [
    slot("role", FL2V_GUIDE_ROLE, true),
    slot("task", FL2V_GUIDE_TASK, true),
    slot("process", FL2V_GUIDE_PROCESS, true),
    slot("principles", FL2V_GUIDE_PRINCIPLES, true),
    slot("output", FL2V_GUIDE_OUTPUT, false),
  ],
  buildFullPrompt(sc) {
    // 同样逻辑，resolve slots
  },
};
```

#### 3.1.2 `video_h3_fl2v_content` — FL2V Content Layer Labels

```typescript
// 将 buildContentLayer() 中的静态标签文本注册为 slots
// 动态数据（videoScript, character names, dialogue text）由引擎注入

const FL2V_CONTENT_SCRIPT_LABEL = `## 视频剧本
{{VIDEO_SCRIPT}}`;

const FL2V_CONTENT_CHAR_LABEL = `## 角色
（这些角色已出现在首尾帧中。仅描述他们的动作和对话，不要描述外貌。）
分配说话人 ID（按出场顺序：S1, S2, ...）：
{{CHARACTER_LIST}}`;

const FL2V_CONTENT_DIALOGUE_LABEL = `## 对话台本（必须在视频中呈现！）
{{DIALOGUE_LIST}}`;

const FL2V_CONTENT_FRAME_LABEL = `## 帧锚点（关键帧图片）
以下是实际用作首/末帧锚点的图片。你只需了解角色的位置和构图——不要描述环境/光线/物件细节（图片已提供）。
{{FRAME_ANCHORS}}`;

// ⚠️ SCENE 段已删除 — FL2V 模式下首尾帧图片已提供全部环境信息

const FL2V_CONTENT_EPISODE_LABEL = `## 剧集背景
{{EPISODE_CONTEXT}}`;

const FL2V_CONTENT_AUDIO_LABEL = `## 音频
{{AUDIO_CONTEXT}}`;

const FL2V_CONTENT_NARRATION_HINT = `## 声音提示
如果本 shot 无对白或对白不足，你必须添加画外音/旁白：
- 旁白格式：(S0) says in an off-screen voiceover: <d>[Chinese] 文本</d>
- 内心独白格式：(S1) says in an off-screen voiceover: <d>[Chinese] 文本</d> while his lips remain completely closed
每 3-5 秒至少插入一句。`;

const fl2vContentDef: PromptDefinition = {
  key: "video_h3_fl2v_content",
  category: "video",
  slots: [
    slot("script_label", FL2V_CONTENT_SCRIPT_LABEL, false),
    slot("character_label", FL2V_CONTENT_CHAR_LABEL, true),
    slot("dialogue_label", FL2V_CONTENT_DIALOGUE_LABEL, false),
    slot("frame_label", FL2V_CONTENT_FRAME_LABEL, true),
    slot("episode_label", FL2V_CONTENT_EPISODE_LABEL, false),
    slot("audio_label", FL2V_CONTENT_AUDIO_LABEL, false),
    slot("narration_hint", FL2V_CONTENT_NARRATION_HINT, true),
  ],
  buildFullPrompt(sc) {
    // ...
  },
};
```

#### 3.1.3 `video_h3_fl2v_constraints` — FL2V Constraint Rules

```typescript
// 将 buildConstraintLayer() 的全部硬规则注册为 slots

const FL2V_CONSTRAINT_TIME_STRUCTURE = `【时间结构 — 强制执行】
1. 必须按 {{SEGMENT_COUNT}} 个时间段拆分（不要创建 [Shot 2]）
2. 每个时间段必须有独立的视觉变化和运镜动作`;

const FL2V_CONSTRAINT_ACTION_BEATS = `【动作节拍 — 强制执行】
3. 每 2-3 秒安排一个微动作节点——即使静态镜头也要加入：呼吸节奏、衣物飘动、光线变化、水面波动、镜头微调
4. 用「先...随即...然后...最终...」串联微动作，禁止把全部动作写成同时发生`;

const FL2V_CONSTRAINT_CAMERA = `【运镜 — 第一优先级】
7. 每个时间段首句必须是运镜动作：
   格式："镜头 [运动类型] [幅度] [速度]"
   例："镜头极缓慢推近，小幅度。"
   运镜写完后，才写角色动作。
8. 运镜必须含幅度+速度修饰
9. 主运镜方向：{{CAMERA_DIRECTION}}`;

const FL2V_CONSTRAINT_DIALOGUE = `【对白 — 强制执行】
5. 对白格式：(S1)说：<d>[中文] 原文台词</d>
   画外音格式：(S1) says in an off-screen voiceover: <d>[Chinese] text</d>
6. 对白必须嵌入对应时间段——先描述角色动作，再写对白行`;

const FL2V_CONSTRAINT_FORMAT = `【格式】
10. 角色已在帧中——仅描述动作和移动，禁止描述外貌
11. 禁止 markdown、代码块、注释——纯 H3 格式输出
12. 禁止逐字复制剧本——转换为丰富的影视级散文`;

const FL2V_CONSTRAINT_NO_ENV = `【环境 — FL2V 专属规则】
13. 首尾帧图片已提供全部环境/光线/物件。禁止在 prompt 中描述：
    - 静态场景元素（建筑、家具、自然景物）
    - 静态光线条件（光位/色温/质感）
    - 静态物件细节（道具、装饰、纹理）
14. 只描述环境变化：火焰忽明忽暗 ✅ / 云遮月导致光线暗下 ✅ /
    破庙内篝火舞蹈映照土墙 ❌ / 暖橘光从左低角照亮 ❌`;

const FL2V_CONSTRAINT_BODY_VOCAB = `【身体动作 — 白名单】
15. 使用具体物理动词：转头、抬眼、垂眼、握紧、松开、抬手、放手、
    迈步、后退、前倾、后仰、起身、坐下、跪地、站起、转体、眯眼、眨眼
16. 禁止抽象词："陷入沉思"→"眼帘低垂，眉心微蹙"
    禁止模糊词："神情变化"→"眉头从紧锁渐转为舒展"`;

const FL2V_CONSTRAINT_VOICE = `【声音 — 主动补位】
17. 如果本 shot 对话台本为空，必须主动生成画外音或旁白
18. 每 3-5 秒至少一句声音。禁止纯默片 shot。`;

const fl2vConstraintsDef: PromptDefinition = {
  key: "video_h3_fl2v_constraints",
  category: "video",
  slots: [
    slot("time_structure", FL2V_CONSTRAINT_TIME_STRUCTURE, true),
    slot("action_beats", FL2V_CONSTRAINT_ACTION_BEATS, true),
    slot("camera", FL2V_CONSTRAINT_CAMERA, true),
    slot("dialogue", FL2V_CONSTRAINT_DIALOGUE, true),
    slot("format", FL2V_CONSTRAINT_FORMAT, true),
    slot("no_env", FL2V_CONSTRAINT_NO_ENV, true),       // ← 新增
    slot("body_vocab", FL2V_CONSTRAINT_BODY_VOCAB, true), // ← 新增
    slot("voice", FL2V_CONSTRAINT_VOICE, true),           // ← 新增
  ],
  buildFullPrompt(sc, params) {
    const s = this.slots;
    const r = (k: string) => resolve(sc, s, k);
    let text = [
      "## 约束规则",
      "",
      r("time_structure"),
      "",
      r("action_beats"),
      "",
      r("dialogue"),
      "",
      r("camera"),
      "",
      r("format"),
      "",
      r("no_env"),
      "",
      r("body_vocab"),
      "",
      r("voice"),
    ].join("\n");
    // 动态变量替换
    text = text.replace("{{SEGMENT_COUNT}}", String(params?.segmentCount ?? 3));
    text = text.replace("{{CAMERA_DIRECTION}}", String(params?.cameraDirection ?? "static"));
    return text;
  },
};
```

#### 3.1.4 `video_h3_fl2v_narration` — 旁白生成 System Prompt

```typescript
const FL2V_NARRATION_SYSTEM = `你是一位历史剧旁白编剧。给定一个镜头（Shot）的上下文，为它撰写一段叙事声音。

输入：
- 镜头视频脚本（videoScript）
- 剧集背景（episode description）
- 出场角色列表

输出要求：
- 生成 1-3 句声音内容
- 类型：旁白（第三人称叙述者 S0）或内心独白（角色 offscreen voiceover S1/S2）
- 格式：(S0) says in an off-screen voiceover: <d>[Chinese] text</d>
- 旁白应解说背景、推进叙事、揭示内心冲突
- 内心独白应自然、口语化，符合角色性格和当前情绪
- 语言：中文

仅输出声音行，不要前言/解释/markdown。`;

const fl2vNarrationDef: PromptDefinition = {
  key: "video_h3_fl2v_narration",
  category: "video",
  slots: [
    slot("system", FL2V_NARRATION_SYSTEM, true),
  ],
  buildFullPrompt(sc) {
    return resolve(sc, this.slots, "system");
  },
};
```

#### 3.1.5 `video_h3_r2v_guide` — R2V Guide Layer（占位）

```typescript
// 保留现有 video_h3_prompt 内容作为 R2V guide
// 不改行为，只改名 + 拆 slot

const r2vGuideDef: PromptDefinition = {
  key: "video_h3_r2v_guide",
  // ... 内容从当前 video_h3_prompt 迁移
};
```

#### 3.1.6 `video_h3_t2v_guide` — T2V Guide Layer（占位）

```typescript
// 空实现，抛出 NotImplemented
const t2vGuideDef: PromptDefinition = {
  key: "video_h3_t2v_guide",
  category: "video",
  slots: [],
  buildFullPrompt() { throw new Error("T2V not implemented yet"); },
};
```

### 3.2 旧条目处理

| 旧 key | 处理方式 |
|--------|---------|
| `video_h3_prompt` | 保留 → 内部转发到 `video_h3_fl2v_guide`（向后兼容） |
| `video_generate` | 保留（Seedance 旧路径仍可用） |
| `ref_video_generate` | 保留 |

---

## 4. h3/ 目录重构 — 文件变更明细

### 4.1 文件迁移清单

```
Phase 1a: 创建新目录
  mkdir h3/fl2v/ h3/r2v/ h3/t2v/ h3/shared/

Phase 1b: 提取共享
  h3/base-mode.ts → h3/shared/base-builder.ts
    └─ 只提取 buildH3BasePrompt() [local formatter]
    └─ buildH3BasePromptLLM() 留在原处（FL2V 专属）

Phase 1c: FL2V 迁移
  h3/base-mode.ts → h3/fl2v/builder.ts
    └─ buildH3BasePromptLLM → buildFL2VPromptLLM
    └─ buildH3BasePrompt  → 改为从 shared/base-builder 导入
  h3/prompt-template.ts → h3/fl2v/prompt-template.ts
    └─ buildContentLayer, buildConstraintLayer, buildGuideLayer → FL2V 专属

Phase 1d: R2V 迁移
  h3/ref-mode.ts → h3/r2v/ref-builder.ts
    └─ buildH3Ref2VAPrompt → buildR2VPrompt
    └─ 导入 buildH3Sections 从 ../shared/base-builder

Phase 1e: 重写入口
  h3/index.ts → 模式分发器
    buildVideoPromptLLM(input, provider, systemOverride)
      switch (input.generationMode):
        "reference" → r2v/ref-builder.buildR2VPrompt(input)
        "keyframe"  → fl2v/builder.buildFL2VPromptLLM(input, provider, systemOverride)

Phase 1f: T2V 占位
  h3/t2v/index.ts → throw new Error("T2V not implemented")
```

### 4.2 关键文件内容

#### `h3/index.ts` — 模式分发器

```typescript
import type { AIProvider } from "@/lib/ai/types";
import type { H3PromptInput, H3PromptOutput } from "./types";

/**
 * Build H3 prompt via LLM (production path).
 * Dispatches to mode-specific builder based on input.generationMode.
 */
export async function buildVideoPromptLLM(
  input: H3PromptInput,
  textProvider: AIProvider,
  systemOverride?: string
): Promise<H3PromptOutput> {
  if (input.generationMode === "reference") {
    const { buildR2VPrompt } = await import("./r2v/ref-builder");
    return buildR2VPrompt(input);
  }
  // Default: FL2V (keyframe mode)
  const { buildFL2VPromptLLM } = await import("./fl2v/builder");
  return buildFL2VPromptLLM(input, textProvider, systemOverride);
}

/** Local builder (no LLM — fast fallback) */
export function buildVideoPrompt(input: H3PromptInput): H3PromptOutput {
  if (input.generationMode === "reference") {
    const { buildR2VPrompt } = require("./r2v/ref-builder");
    return buildR2VPrompt(input);
  }
  const { buildFL2VPrompt } = require("./fl2v/builder");
  return buildFL2VPrompt(input);
}

export type { H3PromptInput, H3PromptOutput };
```

#### `h3/fl2v/builder.ts` — FL2V LLM + 本地 Builder

```typescript
import type { AIProvider } from "@/lib/ai/types";
import type { H3PromptInput, H3PromptOutput } from "../types";
import { buildH3Sections } from "../shared/base-builder";
import { buildFL2VPromptTemplate } from "./prompt-template";

/**
 * FL2V LLM builder.
 * Reads Guide Layer from registry (or systemOverride).
 * Content + Constraint layers assembled from registry slots in prompt-template.
 */
export async function buildFL2VPromptLLM(
  input: H3PromptInput,
  textProvider: AIProvider,
  systemOverride?: string
): Promise<H3PromptOutput> {
  const lang = resolveLanguage(input);
  try {
    const { system, user } = await buildFL2VPromptTemplate(input, systemOverride);

    const raw = await textProvider.generateText(user, {
      systemPrompt: system,
      temperature: 0.7,
      maxTokens: 32000,
    });

    if (!raw?.trim()) throw new Error("Empty LLM response");
    return parseLLMSections(raw, input, lang);
  } catch (e) {
    console.warn("[H3-FL2V] LLM failed, using local fallback:", (e as Error).message);
    return buildFL2VPrompt(input);
  }
}

/** Local fallback — no LLM */
export function buildFL2VPrompt(input: H3PromptInput): H3PromptOutput {
  const lang = resolveLanguage(input);
  return buildH3Sections(input, lang);
}
```

#### `h3/fl2v/prompt-template.ts` — 读取 Registry 的模板引擎

```typescript
import { resolvePrompt, resolveSlotContents } from "@/lib/ai/prompts/resolver";
import { mapCameraDirection } from "../camera-map";
import { computeSegments } from "./time-segments";
import type { H3PromptInput, H3Language } from "../types";

/**
 * FL2V 3-layer prompt template.
 * Layer 1 (Content) + Layer 2 (Constraints) → user message
 * Layer 3 (Guide) → system message
 *
 * All static text comes from registry slots, NOT hardcoded.
 */
export async function buildFL2VPromptTemplate(
  input: H3PromptInput,
  systemOverride?: string
): Promise<{ system: string; user: string }> {
  const lang = resolveLanguage(input);

  // ── Layer 3: Guide (system prompt) ──
  let system = systemOverride;
  if (!system) {
    system = await resolvePrompt("video_h3_fl2v_guide", {})
      .catch(() => resolvePrompt("video_h3_prompt", {}))  // fallback to legacy
      .catch(() => buildHardcodedGuideLayer(lang));       // ultimate fallback
  }

  // ── Layer 1: Content (user message, dynamic data injected) ──
  const contentSlots = await resolveSlotContents("video_h3_fl2v_content", {});
  const userContent = buildContentLayer(input, contentSlots, lang);

  // ── Layer 2: Constraints (user message, from registry) ──
  const segments = computeSegments(input.duration || 10);
  const constraintSlots = await resolveSlotContents("video_h3_fl2v_constraints", {});
  const userConstraints = buildConstraintLayer(
    input, constraintSlots, lang, segments
  );

  return {
    system,
    user: [userContent, "", userConstraints].join("\n"),
  };
}
```

---

## 5. 实施步骤

### Phase 1: 目录重构 + Registry 拆分（1h）

| 步骤 | 操作 | 涉及文件 | 验证 |
|------|------|---------|------|
| 1.0 | 创建新目录 | `mkdir h3/{fl2v,r2v,t2v,shared}` | — |
| 1.1 | 提取 `buildH3BasePrompt` 到 `shared/base-builder.ts` | 从 `base-mode.ts` 提取，导出 `buildH3Sections` | — |
| 1.2 | 迁移 `base-mode.ts` → `fl2v/builder.ts` | 重命名导出，导入 `shared/base-builder` | — |
| 1.3 | 迁移 `prompt-template.ts` → `fl2v/prompt-template.ts` | 文件移动，内部引用更新 | — |
| 1.4 | 迁移 `ref-mode.ts` → `r2v/ref-builder.ts` | 导入路径改为 `../shared/base-builder` | — |
| 1.5 | 重写 `h3/index.ts` 为分发器 | 见 §4.2 | — |
| 1.6 | 创建 `t2v/index.ts` 占位 | `throw NotImplemented` | — |
| 1.7 | 更新 4 个 call site 导入路径 | `video-generate.ts` ×2, `route.ts` ×3 | — |
| 1.8 | `tsc --noEmit` | 项目根 | 零错误 |

### Phase 2: Registry 条目注册（30min）

| 步骤 | 操作 | 涉及文件 | 验证 |
|------|------|---------|------|
| 2.1 | 新增 `video_h3_fl2v_guide` | `registry.ts` 追加 6 slots | WebUI 可见 |
| 2.2 | 新增 `video_h3_fl2v_content` | `registry.ts` 追加 7 slots | WebUI 可见 |
| 2.3 | 新增 `video_h3_fl2v_constraints` | `registry.ts` 追加 8 slots (含 3 个新增规则) | WebUI 可见 |
| 2.4 | 新增 `video_h3_fl2v_narration` | `registry.ts` 追加 1 slot | WebUI 可见 |
| 2.5 | 注册 `video_h3_r2v_guide`（从旧 `video_h3_prompt` 迁移内容） | `registry.ts` | WebUI 可见 |
| 2.6 | 注册 `video_h3_t2v_guide`（占位） | `registry.ts` | WebUI 可见 |
| 2.7 | 更新 `video_h3_prompt` → 转发到 `video_h3_fl2v_guide` | `registry.ts` `buildFullPrompt` | 向后兼容 |
| 2.8 | `tsc --noEmit` | 项目根 | 零错误 |

### Phase 3: FL2V 引擎接入 Registry（1h）

| 步骤 | 操作 | 涉及文件 | 验证 |
|------|------|---------|------|
| 3.1 | `fl2v/prompt-template.ts` 改为从 registry 读取 slots | `buildFL2VPromptTemplate()` | — |
| 3.2 | `fl2v/builder.ts` 使用新的 `buildFL2VPromptTemplate` | `buildFL2VPromptLLM()` | — |
| 3.3 | Content Layer 裁剪 — 删除 SCENE 段 | `buildContentLayer()` | — |
| 3.4 | Content Layer 裁剪 — FRAME ANCHORS 只保留 [shot]+[subject] | `buildContentLayer()` | — |
| 3.5 | 日志前缀 `[H3-FL2V]` | `builder.ts` | — |
| 3.6 | `tsc --noEmit` + server restart | — | 零错误 |
| 3.7 | 跑一次 EP02 批量 frame generate → 观察新 prompt 效果 | WebUI | — |

### Phase 4: 旁白自动生成（2h，P0/P1 验证后）

| 步骤 | 操作 | 涉及文件 |
|------|------|---------|
| 4.1 | 新增 `fl2v/narration-gen.ts` | 调用 LLM 生成旁白 |
| 4.2 | `fl2v/builder.ts` 挂载：无对白 shot → 触发生成 | `buildFL2VPromptLLM()` |
| 4.3 | 旁白注入 Content Layer | `buildContentLayer()` |

---

## 6. i18n 扩展

```jsonc
// messages/zh.json 新增
{
  "promptTemplates": {
    "prompts": {
      "videoH3Fl2vGuide": "H3 FL2V Guide 层",
      "videoH3Fl2vContent": "H3 FL2V Content 层",
      "videoH3Fl2vConstraints": "H3 FL2V 约束规则",
      "videoH3Fl2vNarration": "H3 FL2V 旁白生成",
      "videoH3R2vGuide": "H3 R2V Guide 层"
    },
    "slots": {
      // FL2V Guide
      "fl2vGuideRole": "角色定义",
      "fl2vGuideTask": "任务说明",
      "fl2vGuideProcess": "处理流程",
      "fl2vGuidePrinciples": "关键原则",
      "fl2vGuideOutput": "输出格式",
      // FL2V Content
      "fl2vContentScriptLabel": "视频剧本标签",
      "fl2vContentCharLabel": "角色列表标签",
      "fl2vContentDialogueLabel": "对白列表标签",
      "fl2vContentFrameLabel": "帧锚点标签",
      "fl2vContentEpisodeLabel": "剧集背景标签",
      "fl2vContentAudioLabel": "音频标签",
      "fl2vContentNarrationHint": "旁白提示",
      // FL2V Constraints
      "fl2vConstraintTimeStructure": "时间结构规则",
      "fl2vConstraintActionBeats": "动作节拍规则",
      "fl2vConstraintCamera": "运镜规则",
      "fl2vConstraintDialogue": "对白规则",
      "fl2vConstraintFormat": "格式规则",
      "fl2vConstraintNoEnv": "环境禁止规则 [FL2V专属]",
      "fl2vConstraintBodyVocab": "身体动词语汇 [FL2V专属]",
      "fl2vConstraintVoice": "声音主动补位 [FL2V专属]",
      // Narration
      "fl2vNarrationSystem": "旁白系统提示"
    }
  }
}
```

---

## 7. 向后兼容性保证

| 项目 | 兼容策略 |
|------|---------|
| `h3/index.ts` 公共 API | `buildVideoPromptLLM(input, provider, systemOverride)` 签名完全不变 |
| `video-generate.ts` / `route.ts` | `import { buildVideoPromptLLM } from "@/lib/ai/prompts/h3"` 路径不变 |
| `resolvePrompt("video_h3_prompt")` | 内部转发到新 `video_h3_fl2v_guide`，用户已保存的自定义 prompt 继续生效 |
| `video_h3_prompt` registry entry | 保留在 registry 中，`buildFullPrompt` 转发 |
| Seedance 旧路径 | `video_generate` / `ref_video_generate` 条目不动 |
| H3_PROMPT_MODE=seedance | `video-generate.ts` 中的分支逻辑不动，旧路径原样保留 |

---

## 8. 验证清单

- [ ] `tsc --noEmit` 零错误
- [ ] WebUI 提示词模板页面出现 6 个新条目
- [ ] 每个新条目可展开看到独立 slots
- [ ] 修改 slot 内容 → 下次生成 prompt 时生效
- [ ] EP02 批量 frame generate → prompt 中无环境描述
- [ ] EP02 批量 frame generate → prompt 中有运镜优先结构
- [ ] EP02 批量 frame generate → 无对白 shot 有旁白/画外音
- [ ] `H3_PROMPT_MODE=seedance` 旧路径不受影响
- [ ] generationMode="reference" 的 R2V 路径不受影响