# P4 提示词成本精简清单（待审核）

> 目标：在不改变功能的前提下，降低三个最重 key 的 system 提示词体积，减少 LLM token 成本。
> 行号基于 `src/lib/ai/prompts/registry.ts`（含 P1/P2/P3 改动后的当前行号）。
> 原则：只删冗余/重叠/多余示例，保留模型特化防护（Qwen 字面渲染陷阱、镜头保真规则）。

---

## 1. `character_extract` → `CHAR_EXTRACT_DESCRIPTION_REQUIREMENTS`（registry.ts L739-812）

| 行号 | 内容 | 处置 | 说明 / 预估省 |
|---|---|---|---|
| L808-809 | 【补充示例——极端瘦弱】 | 砍 | 与 L757-760 的"瘦"规则（含 ✅/❌ 对比）重复，示例仅作对照。省约 230 字符 |
| L811-812 | 【补充示例——极端肥胖】 | 砍 | 与 L761-764 的"胖/健壮"规则重复。省约 230 字符 |
| L805-806 | 【示例】赛博朋克 | 保留 | 主力完整示例，覆盖全字段 |
| L748-755 | Qwen 2512 字面渲染陷阱词表（危险词/安全词） | 保留 | 模型特化防护，是此提示词的核心价值 |

小计：约 **460 字符（~4% of 11948）**

## 2. `shot_split` → `SHOT_SPLIT_FIDELITY_RULES`（registry.ts L1753-1845）

| 行号 | 内容 | 处置 | 说明 / 预估省 |
|---|---|---|---|
| L1780-1786 | 【自检清单】（5 个 □） | 砍 | 与 L1769-1778「必须 100% 覆盖的内容」7 项高度重叠。省约 250 字符 |
| L1788-1793 | 反例（错误精炼 vs 正确展开） | 保留 | 具体、高价值 |
| L1828-1845 | 战斗/对决强制规则 | 条件化（需代码改动，可选） | 仅在检测到战斗信号词时注入；非战斗剧本不占 token |

小计：约 **250 字符（~2.7% of 9350）**

## 3. `shot_split_keyframe_assets` → `SHOT_KEYFRAME_ASSETS_RULES`（registry.ts L2164-2319）

| 行号 | 内容 | 处置 | 说明 / 预估省 |
|---|---|---|---|
| L2187-2192 | 【角色一致性锚定】 | 砍/合并 | 与 L2174-2176（禁止项 #1：服饰/体型/发型/肤色由参考图锚定）表述重复。省约 350 字符 |
| L2303-2318 | 完整示例（无角色，纯场景） | 砍 | 有角色示例（L2284-2301）已示范标签用法，无角色示例属对照。省约 250 字符 |
| L2284-2301 | 完整示例（有角色） | 保留 |
| L2164-2166 | `physicsRealismBlock()` + `buildStyleMappingBlock()` 共享块 | 保留 | 单一真源，横切防漂移 |

小计：约 **600 字符（~10% of 6140）**

---

## 合计预估
就地删减（纯文本，零 UI 改动）：约 **1300 字符**（三 key 合计 ~27438 字符的 ~5%）。
战斗规则条件化（L1828-1845）是额外的代码改动，单独评估。

## 执行清单（逐条勾选）
- [ ] 1a. 砍 L808-809（极端瘦弱示例）
- [ ] 1b. 砍 L811-812（极端肥胖示例）
- [ ] 2a. 砍 L1780-1786（自检清单，与 100% 覆盖清单重叠）
- [ ] 3a. 合并/砍 L2187-2192（角色一致性锚定，与禁止项 #1 重叠）
- [ ] 3b. 砍 L2303-2318（无角色示例）
- [ ] （可选）条件化 L1828-1845 战斗规则（需代码改动）

## 注意
- 只动"砍"项；所有"保留"项不动。
- 改完跑 `tsc --noEmit` + 重新 `npm run build`，重启 :3030 冒烟。
- 若某条你想保留（例如想留"极端瘦弱/肥胖"对照示例），在清单里勾掉即可，我按你勾的执行。
